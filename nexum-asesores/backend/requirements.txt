# ============================================================
# Nexum Asesores — Dependencias Python
# ============================================================

# ── API ───────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-jose[cryptography]==3.3.0  # JWT
pyjwt==2.8.0
pydantic[email]==2.7.0
httpx==0.27.0                     # cliente HTTP async para testing

# ── Base de datos ─────────────────────────────────────────────
asyncpg==0.29.0                   # driver PostgreSQL async
psycopg2-binary==2.9.9            # driver PostgreSQL sync (Airflow)
sqlalchemy==2.0.30
alembic==1.13.1                   # migraciones

# ── ETL ───────────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
minio==7.2.7                      # cliente MinIO (Bronze)

# ── ML ────────────────────────────────────────────────────────
xgboost==2.0.3
scikit-learn==1.4.2
prophet==1.1.5                    # reemplaza fbprophet
pystan==3.9.1                     # dependencia de Prophet

# ── Airflow ───────────────────────────────────────────────────
# Instalar aparte con extras específicos:
# pip install "apache-airflow==2.9.1" --constraint constraints.txt
# apache-airflow==2.9.1
# apache-airflow-providers-postgres==5.10.0

# ── Seguridad ─────────────────────────────────────────────────
bcrypt==4.1.3                     # hash de contraseñas
cryptography==42.0.7

# ── Utilidades ────────────────────────────────────────────────
python-dotenv==1.0.1
tenacity==8.3.0                   # reintentos automáticos
structlog==24.1.0                 # logging estructurado
